query SidePageMenu($locale: LanguageCodeEnum!, $channel: String!) {
    menu(slug: "mobile-pages", channel: $channel) {
        id
        items {
            ...MenuItemWithChildrenFragment
        }
    }
}
